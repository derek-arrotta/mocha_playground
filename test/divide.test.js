const expect = require('chai').expect;
const divide = require('../index');

describe('Divide function', () => {
  /*
  it('should divide positive integers correctly', () => {
    expect(divide(8, 4)).to.equal(2);
  });

  it('should throw an error when divide by zero', () => {
    expect(() => { divide(8, 0) }).to.throw();
  });

  it('a & b should be a number, not a string', () => {
    expect(() => divide('', '')).to.throw();
  });

  it('a & b should be a number, not a boolean', () => {
    expect(() => divide(true, false)).to.throw();
  });

  it('a & b should be defined', () => {
    expect(() => divide(undeclaredVariable1, undeclaredVariable2)).to.throw();
  });
  */

  /*
  it('should pass test1', () => {
    expect(2).to.equal(2, '2 === 2'); // pass
  })
  it('should pass test2', () => {
    expect(2).to.equal("2", '2 === "2"'); // fail, wrong types
  })
  it('should pass test3', () => {
    expect(2).to.equal(3, '2 === 3'); // fail, wrong values
  })
  it('should pass test4', () => {
    expect('hen').to.equal('Hen', "'hen' === 'Hen'"); // fail, string comparison is case sensitive
  })
  it('should pass test5', () => {
    expect(true).to.equal(true, "true===true"); // 
  })
  it('should pass test6', () => {
    expect(600/5).to.equal(600/10); // 
  })
  */

});