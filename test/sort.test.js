const expect = require("chai").expect;
const sort = require("../sort");

describe("sort function", () => {
  /*

  it("the list should be an array", () => {
    expect(sort([1, 3, 2, 5, 4])).to.be.an('array');
  });

  it("the array values should be numbers", () => {
    list = [1, 3, 2, 5, 4];
    expect(list[1]).to.be.a('number');
  });

  it("the resulting array should be in ascending (i.e. 1,2,3) order", () => {
    expect(sort([1, 3, 2, 5, 4])).to.deep.equal([1, 2, 3, 4, 5]);
  });

  it('empty array throws error', () => {
    expect(() => { sort([]) } ).to.throw();
  });
  */
});
